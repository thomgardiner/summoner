/target
.DS_Store
.debug-kimi/

# Public docs stay tracked (docs/reference.md). Everything else under docs/
# is internal planning, research, or retros — never publish.
docs/plans/
docs/specs/
docs/decisions/
docs/research/
docs/retros/

# Fleet order files are local orchestration, not product surface.
/orders/
examples/demo/.git/
